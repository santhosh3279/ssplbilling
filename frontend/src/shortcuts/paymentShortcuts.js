/**
 * src/shortcuts/paymentShortcuts.js
 * ─────────────────────────────────────────────────────────────────────────────
 * Keyboard shortcuts for the Payment & Receipt page.
 */

export const paymentShortcuts = (handlers) => ({
  'F7': handlers.cycleTab || (() => {}),
})
